build:
  buildpacks:
    - heroku/python
  config:
    PATH: helloworld
    PYTHONPATH: helloworld
  commands:
    - pip install -r helloworld/requirements.txt
run:
  web: gunicorn django_project.wsgi --chdir helloworld
